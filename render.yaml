services:
  - type: web
    name: dnpk-tareas
    runtime: node
    plan: free
    buildCommand: chmod +x build.sh && bash build.sh
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
    autoDeploy: true
